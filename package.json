{
    "name": "Amherst Community Band",
    "scripts": {
        "build": "ELEVENTY_ENV=production npx @11ty/eleventy && ELEVENTY_ENV=production node build.js",
        "build:dev": "npx @11ty/eleventy",
        "serve": "npx @11ty/eleventy --serve"
    },
    "dependencies": {
        "@11ty/eleventy": "^3.1.2",
        "flickity": "^3.0.0"
    },
    "devDependencies": {
        "@11ty/eleventy-img": "^6.0.4",
        "esbuild": "^0.25.10"
    }
}
